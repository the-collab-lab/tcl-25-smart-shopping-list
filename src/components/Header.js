import React from 'react';

import LogoIcon from './LogoIcon';

const Header = () => (
  <header className="app__header" id="logo-icon">
    <LogoIcon />
    <h1>Smart Shopping App</h1>
  </header>
);

export default Header;
