import React, { useEffect, useState } from 'react';

const Home = () => {
  return (
    <main>
      <button className="button">Create a Shopping List</button>
    </main>
  );
};

export default Home;
